## arduino-esp32 as an IDF component via the Component Manager. 3.3.x resolves to
## the latest 3.3.8 at the time of writing; Kconfig range is '>=5.3,<5.6' so it's
## compatible with our IDF v5.4.4.
dependencies:
  idf:
    version: ">=5.4.0"
  espressif/arduino-esp32:
    version: "^3.2.0"
